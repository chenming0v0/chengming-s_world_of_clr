import crafttweaker.item.IItemStack;
import crafttweaker.player.IPlayer;

zenClass IItemTransformer {
    
    function (item as IItemStack, byPlayer as IPlayer) as IItemStack;
    
    function transform(item as IItemStack, byPlayer as IPlayer) as IItemStack;
}